{
  "name": "mock-api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "generate-mock-data": "node generate-mock-data",
    "prestart-mockapi": "npm run generate-mock-data",
    "start-mockapi": "json-server --watch ./db.json --port 3001",
    "start": "npm run start-mockapi",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "json-schema-faker": "^0.4.6"
  }
}
